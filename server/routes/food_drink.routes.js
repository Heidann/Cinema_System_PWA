import { Router } from "express";
import * as foodDrinkController from "../controllers/food_drink.controller.js";

const router = Router();

// router.get("/", foodDrinkController.getAllFoodDrinks);
// router.get("/:id", foodDrinkController.getFoodDrinkById);
// router.post("/", foodDrinkController.createFoodDrink);
// router.put("/:id", foodDrinkController.updateFoodDrink);
// router.delete("/:id", foodDrinkController.deleteFoodDrink);

export default router;
